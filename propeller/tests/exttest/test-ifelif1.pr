# Tests variable assignment and and if, elif, else statement to print on elif
fn init() -> int
{
  bool a;
  bool b;
  a=false;
  b=true;
  if   a
  { print(1); }
  elif b
  { print(2); }
  else
  { print(3); }
  return 0;
}
